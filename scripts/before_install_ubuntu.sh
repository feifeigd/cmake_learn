#!/usr/bin/env sh
# travis-ci.org已经给安装上了3.2.2的cmake了不必安装了
# sudo apt-get remove -y cmake # 卸载旧版本的
# sudo add-apt-repository -y ppa:george-edison55/precise-backports
# sudo apt-get update 
# sudo apt-get install cmake
